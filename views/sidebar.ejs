<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

</head>

<style>
    .sidebar{
        width: 17vw;
        background-color: #2D3748;
        height: 100vh;
        color: #38B2AC;
        font-size: 1.2rem;
        display:flex;
        justify-content: center;
    }

    ul{
        margin-top: 2rem;
        display:flex;
        flex-direction: column;
        gap: 1rem;
        font-size: 1.2rem;
        font-weight: 600;

    }

    aside{
        height: 100vh;
    }

    *{
        margin: 0;
        padding: 0;
    }
 
</style>

<body>
    <aside>
        <div class="sidebar">
            <!-- Folders list rendered dynamically -->
            <ul>
                <% folders.forEach(function(folder) { %>
                    <a href="/?folder=<%=folder.id%>"><li><%= folder.name %></li></a>
                <% }); %>
            </ul>
        </div>
    </aside>
</body>
</html>